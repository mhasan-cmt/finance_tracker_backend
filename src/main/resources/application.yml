spring:
  main:
    allow-circular-references: true
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
#    url: ${DB_URL}
    url: jdbc:postgresql://ep-blue-dust-a14207tk-pooler.ap-southeast-1.aws.neon.tech/fitrack_db
#    username: ${DB_USER}
    username: fitrack_db_owner
#    password: ${DB_PASSWORD}
    password: npg_MtWOvl8aEYo5
  mail:
    host: ${EMAIL_HOST}
    port: 587
    username: ${EMAIL_USER}
    password: ${EMAIL_APP_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwtSecret: ======================expense=tracker=mywallet===========================
  jwtExpirationMs: 86400000
  verificationCodeExpirationMs: 900000
  user:
    profile:
      upload:
        dir: ./uploads/user/profile
